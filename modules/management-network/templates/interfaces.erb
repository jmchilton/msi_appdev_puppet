# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

# The loopback network interface
auto lo
iface lo inet loopback

auto eth0
iface eth0 inet manual

auto br100
iface br100 inet static
        address <%= network[network_node]['management_ip'] %>
        network 172.0.0.0
        netmask 255.255.255.0
        broadcast 172.0.0.255
        bridge_ports eth0
        bridge_fd 0
        bridget_maxwait 0
        bridge_hello 2
        bridge_maxage 12
        bridge_stp off


# The primary network interface
#allow-hotplug eth0
#iface eth0 inet static
#        address <%= network[network_node]['management_ip'] %>
#        netmask 255.255.255.0
#        network 172.0.0.0
#        broadcast 172.0.0.255

# The primary network interface
allow-hotplug eth1
iface eth1 inet static
        address <%= network[network_node]['public_ip'] %>
        netmask 255.255.255.0
        network 128.101.191.0
        broadcast 128.101.191.255
        gateway 128.101.191.254
        # dns-* options are implemented by the resolvconf package, if installed
        dns-nameservers 128.101.101.101 134.84.84.84
        dns-search msi.umn.edu
